import React from 'react'
import styled from 'styled-components'

import Layout from '../../components/templates/layout'

const PostTemplate = ({frontmatter, html}) => (
    <Layout>

    </Layout>
)

export default PostTemplate
